# Beta — production runtime behavior, beta values.
NODE_ENV=production
PORT=3000
LOG_PRETTY=false
LOG_HTTP_BODIES=false
SWAGGER_ENABLED=true
THROTTLE_TTL=60
THROTTLE_LIMIT=100
# DATABASE_URL and JWT_SECRET are deliberately absent: the deployment must
# inject them, and validateEnv fails the boot if it does not.
